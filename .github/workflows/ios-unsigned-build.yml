name: iOS Unsigned Build

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.2"
          bundler-cache: false

      - name: Install CocoaPods
        run: gem install cocoapods

      - name: Download ONNX Model
        run: |
          mkdir -p tools/face_model/models
          curl -L "https://github.com/deepinsight/insightface/releases/download/v0.7/w600k_mbf.onnx" \
            -o tools/face_model/models/arcface_fresh.onnx

      - name: Install Pods
        run: pod install

      - name: Build Unsigned iPhone App
        run: |
          xcodebuild \
            -workspace EyePal.xcworkspace \
            -scheme EyePal \
            -configuration Release \
            -sdk iphoneos \
            -destination 'generic/platform=iOS' \
            -derivedDataPath build \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            clean build

      - name: Package Unsigned IPA
        run: |
          APP_PATH="build/Build/Products/Release-iphoneos/EyePal.app"
          test -d "$APP_PATH"
          rm -rf Payload EyePal-unsigned.ipa
          mkdir -p Payload
          cp -R "$APP_PATH" Payload/EyePal.app
          ditto -c -k --sequesterRsrc --keepParent Payload EyePal-unsigned.ipa

      - name: Upload Artifact
        uses: actions/upload-artifact@v5
        with:
          name: EyePal-unsigned-ipa
          path: EyePal-unsigned.ipa
